


int signalInterrupt() {
	
}

int main(char * args) {
	
	//infinite loop
		//Incriment PC
		//Check if max pc
		//If max PC reset to 0
		//Check if interrupted with trylock
		//If interrupted execute interrupt
		//Check it PC in I/O array
		//If equal call I/O trap handler
	//End loop
	
	return 0;
}